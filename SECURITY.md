# 🛡️ 安全策略与隐私声明 (Security Policy)

本项目高度重视软件的安全性、环境隔离性与用户隐私。以下是我们的安全基线与漏洞报告策略。

---

## 🔍 支持的版本 (Supported Versions)

| 版本 | 是否支持安全更新 | 说明 |
| :--- | :---: | :--- |
| **v0.5.x** | :white_check_mark: 支持 | 当前稳定维护版本 |
| < v0.5.0 | :x: 不支持 | 早期原型版本，请及时升级至最新版 |

---

## 🔒 核心安全承诺与边界保证

1. **绝对零数据收集 (Zero Telemetry)**：
   - 本项目不包含任何形式的用户行为统计、账号分析或日志上报代码；
   - 唯一的网络访问发生在用户**主动点击**【🔍 检测当前节点】时，向公开权威的 GeoIP 服务发出只读 HTTPS 请求以比对当前 IP 与时区。
2. **零账号凭证接触 (No Credential Touching)**：
   - 启动器只负责准备与传递基础环境变量，绝不读取、嗅探、存储或转发用户的 ChatGPT 登录凭据、Cookie、Session Token 或对话内容。
3. **严格白名单变量隔离 (Strict Whitelist Isolation)**：
   - 仅允许读写受信任的 5 个代理及时间环境变量：`TZ`、`HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY`、`NO_PROXY`；
   - 任何涉及其他系统关键变量（如 `PATH`、`COMSPEC`、`USERPROFILE`）的请求将被底层事务引擎直接阻断。
4. **Compare-and-Restore 与凭据脱敏**：
   - 恢复环境变量时若发生外部改值冲突，程序严格保留当前值，并在诊断提示与界面告警中执行变量值脱敏，绝不向屏幕打印代理密码或敏感明文。
5. **完全透明开源 (Auditable Source)**：
   - 项目完全由原生 C# 单文件源码构成，没有任何预编译闭源二进制依赖；
   - 用户可直接使用 Windows 自带的 `csc.exe` 校验和编译源码，确保生成的每 1 字节均完全受控。

---

## 🚨 报告安全漏洞 (Reporting a Vulnerability)

如果你在本项目中发现了安全隐患（例如环境变量未安全清理、越权操作、并发锁失效等）：

1. **请勿直接公开发布 Issue**；
2. 请通过 GitHub 私密安全公告 (Security Advisories) 或向维护者发送邮件反馈详细的复现步骤与 PoC；
3. 我们承诺在收到漏洞报告后 48 小时内完成评估并给出响应修复计划。
